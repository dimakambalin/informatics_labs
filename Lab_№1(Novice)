Novice :
*1. Создайте директорию, создайте файл, поперемещайте и покопируйте их. Попытайтесь удалить созданную директорию.

cd  *переходим в изначальную директорию
mkdir papka_1   *создаём новую директорию
touch new_file  *создаём файл
mv new_file papka_1 *перемещаем файл в созданную директорию
cd papka_1  *переходим в созданную директорию
cp new_file ~ /home/students/17200/kambalin/  *копируем созданный файл из созданной директории в изначальную директорию
cd  *переходим в изначальную директорию
rm -rf new_file *удаляем новый файл, который мы скопирывали из новой директории в изначальную директорию
rm -rf papka_1  *удаляем созданную директорию с её содержимым 
*2. Домашняя директория : /home/students/17200/kambalin
*3. Команда cd без параметров переносит нас в домашнюю директорию
*4. Команда cat соединяет и выводит файлы -> чтение предыдущих команд возможно; команда less позволяет просматривать длинные тексты, 
которые не вмещаются на одном экране -> текст накладывается на все предыдущие записи в терминале так, что их становится не видно
